* text=auto

# Unity YAML assets remain text for diffs and merges.
*.meta text eol=lf
*.unity text eol=lf
*.prefab text eol=lf
*.asset text eol=lf
*.mat text eol=lf
*.anim text eol=lf
*.controller text eol=lf
*.asmdef text eol=lf
*.json text eol=lf
*.lua text eol=lf
*.cs text eol=lf
*.md text eol=lf

# ---------------------------------------------------------------------------
# Git LFS 已停用（2026-09-19）。
# 原因：GitHub 免费版 LFS 配额为 1 GB 存储 / 1 GB 月流量，而本仓库仅
# fbx/blend/obj 就有 3145 个文件、约 780 MB，加上 png/jpg/tga/wav 等远超配额，
# 推送会被拒绝；同时本机沙箱不允许 LFS 过滤器启动 sh.exe。
# 目前按普通 Git 提交（最大单文件约 24 MB，远低于 GitHub 的 100 MB 单文件上限）。
# 若日后要改回 LFS：取消下面各行的注释，并执行 `git lfs install`，但需先确认
# GitHub LFS 配额（可能需要购买 data pack），且历史需要重写。
# ---------------------------------------------------------------------------
# *.fbx filter=lfs diff=lfs merge=lfs -text
# *.blend filter=lfs diff=lfs merge=lfs -text
# *.obj filter=lfs diff=lfs merge=lfs -text
# *.psd filter=lfs diff=lfs merge=lfs -text
# *.tga filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.jpeg filter=lfs diff=lfs merge=lfs -text
# *.exr filter=lfs diff=lfs merge=lfs -text
# *.wav filter=lfs diff=lfs merge=lfs -text
# *.mp3 filter=lfs diff=lfs merge=lfs -text
# *.ogg filter=lfs diff=lfs merge=lfs -text
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.mov filter=lfs diff=lfs merge=lfs -text
# *.dll filter=lfs diff=lfs merge=lfs -text
